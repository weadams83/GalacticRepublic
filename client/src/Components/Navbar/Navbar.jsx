const Navbar = () => {
  return (
    <div>
      <nav>
        <a href="#users">Users</a>
        <a href="#projects">Projects</a>
        <a href="#teams">Teams</a>
        <input type="text" />
      </nav>
    </div>
  );
};

export default Navbar;
